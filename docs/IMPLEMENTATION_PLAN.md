# HTML и CSS

1. ✅ Создать семантические страницы `index.html` и `menu.html` с общими `header`, навигацией, переключателем темы и `footer`.
2. ✅ Добавить в `index.html` секции Hero, Favorites Coffee, About и Mobile App по структуре Figma и `DESIGN.md`.
3. ✅ Добавить в `menu.html` заголовок, переключатели Coffee/Tea/Dessert, контейнер каталога, кнопку показа дополнительных карточек и контейнер modal.
4. ✅ Подключить локальные шрифты и создать базовые стили в `src/styles/fonts.css`, `reset.css`, `tokens.css`, `base.css`, `layout.css` и `utilities.css`.
5. ✅ Реализовать общие компоненты в `src/styles/components/`: header, footer, buttons, cards, modal и theme toggle.
6. ✅ Реализовать композицию Home и Menu в `src/styles/pages/home.css` и `src/styles/pages/menu.css`.
7. ✅ Добавить адаптивные состояния для 1440, 768 и 380 px, burger layout, сетки карточек и modal без горизонтальной прокрутки.
8. ✅ Добавить светлую тему и подготовить структуру CSS-токенов для последующего подключения тёмной темы.
9. ✅ Подготовить `public/data/products.json` и перенести необходимые изображения, SVG-иконки, favicon и шрифты в предусмотренные каталоги assets.

# Завершение первой части

10. ✅ Добавить тёмную тему в `src/styles/tokens.css` и проверить её применение ко всем секциям Home и Menu.
11. ✅ Добавить один JavaScript-файл для переключения темы, синхронизации `aria-pressed`, сохранения выбора в `localStorage` и восстановления темы после перезагрузки и перехода между страницами.
12. ✅ Вывести на странице Menu минимум восемь карточек категории Coffee из `public/data/products.json`; каждая карточка должна содержать изображение, название, описание и цену.
13. ✅ Отображать кнопку показа дополнительных карточек на Tablet и Mobile; обработчик кнопки допускается реализовать во второй части.
14. ✅ Подключить существующие SVG-иконки к логотипу, ссылке Menu, категориям, стрелкам слайдера, кнопке дополнительных карточек, ссылкам магазинов и социальным ссылкам.
15. ✅ Добавить плавные `hover`, `active` и `focus-visible` состояния ссылкам, кнопкам и карточкам без смещения соседних элементов.
16. ✅ Проверить обе страницы на ширине 1440, 768 и 380 px, а также на промежуточных значениях: без горизонтальной прокрутки, перекрытий и обрезанного контента.
    Проверено 16.09.2026 в Chrome 152 через Chrome DevTools на локальном HTTP-сервере: Home и Menu с загруженными карточками Coffee, светлая тема — 1440, 1280, 1101, 1100, 1024, 900, 800, 769, 768, 700, 601, 600, 480 и 380 px; тёмная тема — 1440, 1024, 768, 600 и 380 px. Проверены границы элементов и текста, перекрытия соседних блоков и полные скриншоты на контрольных ширинах; для визуальной проверки дождались загрузки изображений. Горизонтальная прокрутка, непредусмотренные перекрытия и обрезанный контент не обнаружены, ошибок и предупреждений в Console нет. Исправления HTML/CSS не потребовались. Проверка относится к текущей вёрстке; интерактивные состояния из пунктов 20–24 проверяются после их реализации.
17. Сверить Home и Menu с макетами Coffee House: композицию, сетку, отступы, типографику, изображения, цвета и радиусы.
18. Проверить обе страницы через W3C Validator, а также Console и Network на отсутствие ошибок, 404 и лишних запросов.
19. Подготовить сдачу первой части: репозиторий `rsschool-landing-page`, ветку `landing-page`, публичный деплой и Pull Request в `main` без merge.

# Вторая часть: JavaScript

20. ✅ Создать ветку `landing-page-part-2` от актуального состояния ветки `landing-page`.
21. ✅ Реализовать доступное burger menu на обеих страницах:
    - открывать и плавно закрывать меню по кнопке, синхронизировать `aria-expanded` и превращать burger в крестик;
    - блокировать прокрутку страницы при открытом меню и восстанавливать её после закрытия;
    - закрывать меню по ссылке и клавише `Escape`;
    - при переходе на ширину 769 px и больше закрывать меню, восстанавливать основную навигацию и фокус.
22. ✅ Реализовать циклический Favorites Coffee slider:
    - переключать слайды кнопками Previous и Next в обоих направлениях;
    - плавно переходить между тремя слайдами, включая переходы от последнего к первому и от первого к последнему;
    - синхронизировать активный слайд, видимость элементов, `aria-current` и индикаторы;
    - сохранять корректное состояние на ширине 1440, 768 и 380 px и после изменения ширины окна.
23. ✅ Реализовать динамический каталог из `public/data/products.json`:
    - формировать карточки всех категорий с помощью JavaScript;
    - при загрузке показывать первую категорию и переключать категории без перезагрузки страницы;
    - на ширине 768 px и меньше сначала показывать четыре карточки и отображать кнопку только при наличии скрытых позиций;
    - по кнопке показывать все оставшиеся карточки, а при смене категории или layout сбрасывать состояние.
24. Реализовать product modal из того же объекта `products.json`, из которого создана выбранная карточка:
    - открывать modal по нажатию на любую часть карточки и заполнять изображение, название и описание;
    - блокировать прокрутку страницы и удерживать фокус внутри открытого окна;
    - закрывать modal кнопкой, нажатием на backdrop и клавишей `Escape`, не закрывать при нажатии внутри окна;
    - после закрытия восстанавливать прокрутку и возвращать фокус на исходную карточку.
25. Реализовать параметры продукта и динамический пересчёт цены:
    - при открытии выбирать размер S и оставлять добавки выключенными;
    - разрешать выбор только одного размера и нескольких независимых добавок;
    - визуально выделять выбранные параметры;
    - рассчитывать сумму как базовую цену плюс стоимость размера и выбранных добавок и форматировать результат до двух знаков;
    - при открытии другой карточки полностью сбрасывать параметры и связанную информацию.
26. Добавить понятное пользовательское состояние ошибки загрузки каталога и не оставлять секцию пустой при ошибке `fetch()`.
27. Обеспечить доступность всей интерактивности с клавиатуры, корректные `aria-*` состояния, `focus-visible`, управление фокусом и поддержку `prefers-reduced-motion`.

# Тестирование и итоговая проверка

28. Добавить подходящие проверки для расчёта цены, темы, burger, slider, каталога и modal. Автоматически проверить чистые вычисления и основные пользовательские сценарии, если тестовая среда проекта доступна.
29. Проверить burger на обеих страницах: открытие и закрытие, ссылки, `Escape`, блокировку прокрутки и переход между шириной 768 и 769 px.
30. Проверить slider в обоих направлениях, циклические переходы, индикаторы, плавную смену слайдов и пересчёт состояния после изменения ширины окна.
31. Проверить все категории каталога, начальное количество карточек, кнопку дополнительных карточек и сброс состояния после смены категории и ширины.
32. Проверить modal для товаров каждой категории: заполнение данных, все способы закрытия, удержание и возврат фокуса, блокировку прокрутки и обе цветовые темы.
33. Проверить расчёт цены для каждого размера, одной и нескольких добавок, а также сброс параметров при открытии другого товара.
34. Проверить всю интерактивность мышью и клавиатурой на ширине 1440, 768 и 380 px и на промежуточных значениях.
35. Проверить семантику, HTML Validator, отсутствие горизонтальной прокрутки, обе темы, Console/Network без ошибок и 404, а также визуальное соответствие Figma.

# Сдача второй части

36. Обновить публичный деплой и проверить главную страницу, каталог и всю интерактивность в режиме инкогнито.
37. Подготовить Pull Request из `landing-page-part-2` в `landing-page` без merge и закрытия; в описании указать вариант Coffee House и ссылку на актуальный деплой.
38. Перед отправкой на Cross-Check проверить итоговый diff, Git-статус, отсутствие секретов и временных файлов, а также актуальность требований задания.
